<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#F8FAFC"
            BorderBrush="#D9DEE7"
            BorderThickness="1"
            CornerRadius="4"
            Padding="10"
            Margin="0,0,0,12">
        <StackPanel>
            <TextBlock Text="Opacity"
                       FontWeight="SemiBold"
                       Margin="0,0,0,6" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="185" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <CheckBox Grid.Row="0"
                          Grid.Column="0"
                          Content="Change icon opacity"
                          IsChecked="{Binding EditSettings.ChangeIconOpacity}"
                          Margin="0,5,10,5" />
                <TextBox Grid.Row="0"
                         Grid.Column="1"
                         Text="{Binding EditSettings.IconOpacityText, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding EditSettings.ChangeIconOpacity}"
                         Margin="0,3,6,3"
                         Padding="5"
                         ToolTip="Enter a percentage from 0 to 100" />
                <TextBlock Grid.Row="0"
                           Grid.Column="2"
                           Text="%"
                           VerticalAlignment="Center"
                           Margin="0,0,5,0" />

                <CheckBox Grid.Row="1"
                          Grid.Column="0"
                          Content="Change text opacity"
                          IsChecked="{Binding EditSettings.ChangeLabelOpacity}"
                          Margin="0,5,10,5" />
                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Text="{Binding EditSettings.LabelOpacityText, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding EditSettings.ChangeLabelOpacity}"
                         Margin="0,3,6,3"
                         Padding="5"
                         ToolTip="Enter a percentage from 0 to 100" />
                <TextBlock Grid.Row="1"
                           Grid.Column="2"
                           Text="%"
                           VerticalAlignment="Center"
                           Margin="0,0,5,0" />
            </Grid>

            <TextBlock Text="When opacity is unchecked, changing colour preserves each style's existing transparency."
                       Foreground="#52606D"
                       FontSize="11"
                       TextWrapping="Wrap"
                       Margin="0,6,0,0" />
        </StackPanel>
    </Border>
</UserControl>
